import { DayStatusDirective } from './day-status.directive';

describe('DayStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new DayStatusDirective();
    expect(directive).toBeTruthy();
  });
});
