import { HoursWorkedPipe } from './hours-worked.pipe';

describe('HoursWorkedPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursWorkedPipe();
    expect(pipe).toBeTruthy();
  });
});
